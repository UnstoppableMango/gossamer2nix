# Expected output of the manifest-patching shim (docs/deps/DESIGN.md §3.3)
# applied to ../before/project.toml. Hand-authored spec, not generated.
[project]
id      = "example.com/math"
version = "0.3.1"
edition = "2026"
authors = ["Jane Doe <jane@example.com>"]
license = "Apache-2.0"

[dependencies]
"example.org/linalg" = { path = "/nix/store/00000000000000000000000000000000-linalg" }

[registries]
"example.org" = "https://registry.example.org/v1"
